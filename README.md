# 📊 Ребалансировка портфеля Tinkoff Invest

Многопользовательское веб-приложение для расчета ребалансировки инвестиционного портфеля через Tinkoff Invest API с безопасным хранением токенов.

## 🎯 Возможности

- 🔐 **Безопасное хранение токенов**: Токены шифруются с помощью Fernet (cryptography)
- 👥 **Многопользовательский режим**: Каждый пользователь вводит свой токен через веб-интерфейс
- 📊 **Просмотр портфеля**: Отображение всех счетов и активов с текущей стоимостью
- 🎯 **Ребалансировка**: Расчет необходимых операций для достижения целевого распределения
- 🔄 **Два режима**:
  - **Только покупка**: не продавать активы, только докупать недостающие
  - **Покупка и продажа**: можно продавать переизбыточные активы
- ⚙️ **Управление аккаунтом**: Обновление токена, выход, удаление данных

## 🔒 Безопасность

- ✅ Токены хранятся в зашифрованном виде (Fernet encryption)
- ✅ База данных SQLite с защитой от SQL-инъекций
- ✅ Flask сессии с секретным ключом
- ✅ Возможность удаления токена в любой момент
- ✅ Данные не передаются третьим лицам
- ✅ Все файлы с данными исключены из Git

## 📋 Требования

- **Python 3.9 - 3.13** (рекомендуется 3.11 или 3.12)
  - ⚠️ Python 3.14+ пока не поддерживается из-за несовместимости библиотек
- Токен Tinkoff Invest API (для каждого пользователя)

## 🚀 Установка и запуск

### 1. Клонируйте репозиторий

```bash
cd tinkoff_invest_rebalancer
```

### 2. Установите зависимости

```bash
pip install -r requirements.txt
```

### 3. (Опционально) Настройте переменные окружения

Создайте файл `.env` (необязательно):

   ```bash
# Секретный ключ для Flask сессий (генерируется автоматически, если не указан)
SECRET_KEY=your_secret_key_here

# Ключ шифрования (генерируется автоматически, если не указан)
ENCRYPTION_KEY=your_encryption_key_here

# Порт (по умолчанию 5001)
PORT=5001
```

### 4. Запустите приложение

**Способ 1: Использование скрипта запуска (рекомендуется)**

```bash
./run.sh
```

Скрипт автоматически:
- Создаст виртуальное окружение (если его нет)
- Установит все зависимости
- Проверит версию Python
- Запустит приложение

**Способ 2: Ручной запуск**

```bash
# Создайте виртуальное окружение (опционально, но рекомендуется)
python3 -m venv venv
source venv/bin/activate  # На Windows: venv\Scripts\activate

# Установите зависимости
pip install -r requirements.txt

# Запустите приложение
python app.py
```

Приложение будет доступно по адресу: http://localhost:5001

> 💡 **Примечание**: По умолчанию используется порт 5001, т.к. порт 5000 часто занят службой AirPlay Receiver на macOS.
> 
> Чтобы использовать другой порт:
> ```bash
> export PORT=8080
> python app.py
> ```

## 📖 Как использовать

### Первый вход

1. Откройте приложение в браузере (http://localhost:5001)
2. Введите ваш токен Tinkoff Invest API
3. Опционально укажите ваше имя
4. Нажмите "Войти"

Токен будет зашифрован и сохранен в базе данных.

### Ребалансировка портфеля

1. **Выберите счет** из выпадающего списка
2. **Выберите активы** для ребалансировки, отметив галочками нужные позиции
3. **Укажите целевые доли** для каждого выбранного актива (в процентах, сумма должна быть 100%)
4. **Выберите режим ребалансировки**:
   - **Только покупка**: если вы не хотите продавать активы и готовы внести дополнительные средства
   - **Покупка и продажа**: если вы готовы продать часть активов для ребалансировки
5. **Нажмите кнопку "Рассчитать ребалансировку"**
6. **Изучите результаты**: приложение покажет, сколько каждого актива нужно купить или продать

### Управление токеном

- **Обновить токен**: Настройки → Обновить токен
- **Выйти**: Настройки → Выйти (токен остается в БД)
- **Удалить токен**: Настройки → Удалить токен и данные (полное удаление)

## 💡 Пример использования

Допустим, у вас есть:
- Фонд акций (FXUS): 7000 ₽
- Фонд облигаций (FXRB): 3000 ₽
- **Итого**: 10000 ₽

Вы хотите, чтобы соотношение было 80% акций и 20% облигаций.

### Режим "Только покупка":
- Нужно докупить FXUS на 5000 ₽
- **Дополнительное вложение**: 5000 ₽
- **Итого после**: 15000 ₽ (12000 ₽ акции + 3000 ₽ облигации)

### Режим "Покупка и продажа":
- Продать FXRB на 1000 ₽
- Купить FXUS на 1000 ₽
- **Дополнительное вложение**: 0 ₽
- **Итого после**: 10000 ₽ (8000 ₽ акции + 2000 ₽ облигации)

## 🔒 Архитектура безопасности

### Шифрование токенов

1. Токены шифруются с помощью **Fernet** (симметричное шифрование)
2. Ключ шифрования:
   - Генерируется автоматически при первом запуске
   - Сохраняется в файле `.encryption_key`
   - Может быть указан через переменную окружения `ENCRYPTION_KEY`
3. Зашифрованные токены хранятся в SQLite базе данных

### Сессии пользователей

1. Каждому пользователю присваивается уникальный `session_id`
2. `session_id` хранится в зашифрованной Flask сессии
3. При каждом запросе токен извлекается из БД и дешифруется

### Что НЕ хранится в открытом виде

- ❌ Токены API (только зашифрованные)
- ❌ Пароли (пароли не используются, только токены)

## 📁 Структура проекта

```
tinkoff_invest_rebalancer/
├── app.py                  # Flask приложение с маршрутами
├── tinkoff_service.py      # Сервисы для работы с Tinkoff API
├── auth.py                 # Шифрование и работа с БД
├── requirements.txt        # Зависимости Python
├── .encryption_key         # Ключ шифрования (генерируется автоматически)
├── users.db                # База данных SQLite (создается автоматически)
├── .gitignore              # Защита конфиденциальных файлов
├── run.sh                  # Скрипт запуска
├── README.md               # Документация
├── QUICKSTART.md           # Быстрый старт
└── templates/
    ├── login.html          # Страница входа
    ├── index.html          # Главная страница
    └── settings.html       # Страница настроек
```

## 🛠 Технологии

- **Python 3**: Основной язык
- **Flask**: Веб-фреймворк
- **Tinkoff Invest API**: Для получения данных о портфеле
- **Cryptography (Fernet)**: Шифрование токенов
- **SQLite**: База данных для хранения пользователей
- **HTML/CSS/JavaScript**: Веб-интерфейс

## ⚠️ Ограничения

- Приложение **только рассчитывает** необходимые операции, но **не выполняет** их автоматически
- Вам нужно самостоятельно выполнить рассчитанные операции в приложении Tinkoff
- Цены активов могут измениться между расчетом и выполнением операций
- Не учитываются комиссии брокера

## 📞 Получение токена

1. Откройте приложение Tinkoff Investments
2. Перейдите в настройки
3. Найдите раздел "API для торговли"
4. Создайте новый токен
5. Скопируйте токен (он понадобится при входе в приложение)

## 🐛 Устранение неполадок

**Ошибка "Неверный токен":**
- Проверьте, что токен скопирован полностью без пробелов
- Убедитесь, что токен активен в приложении Tinkoff

**Ошибка "Не авторизован":**
- Ваша сессия истекла, войдите заново
- Перейдите на страницу `/login`

**Ошибка "Address already in use" или "Port 5001 is in use":**
- Порт занят другой программой
- Используйте другой порт: `export PORT=8080 && python app.py`
- Или найдите и остановите программу, использующую порт: `lsof -ti:5001 | xargs kill -9`
- На macOS для порта 5000: отключите AirPlay Receiver в Системных настройках → Основные → AirDrop и Handoff

**Приложение не запускается:**
- Убедитесь, что установлены все зависимости: `pip install -r requirements.txt`
- Проверьте версию Python: `python3 --version` (должна быть 3.9-3.13)
- Если используете Python 3.14+, установите Python 3.11, 3.12 или 3.13

**Ошибка "TypeError: Metaclasses with custom tp_new are not supported":**
- Это означает, что вы используете Python 3.14+, который пока не поддерживается
- Установите Python 3.11, 3.12 или 3.13 и пересоздайте виртуальное окружение:
  ```bash
  rm -rf venv
  python3.12 -m venv venv  # или python3.11, python3.13
  ./run.sh
  ```

**Потерян ключ шифрования:**
- Если вы потеряли файл `.encryption_key`, токены в БД нельзя расшифровать
- Удалите `users.db` и `.encryption_key`, затем перезапустите приложение
- Пользователям нужно будет войти заново

## 🔐 Рекомендации по безопасности

1. **Не передавайте файл `.encryption_key` третьим лицам**
2. **Используйте HTTPS в продакшене** (например, через Nginx с Let's Encrypt)
3. **Не публикуйте `users.db` в открытом доступе**
4. **Регулярно обновляйте зависимости**: `pip install --upgrade -r requirements.txt`
5. **Установите сильный `SECRET_KEY`** для Flask сессий в продакшене
6. **Для продакшена используйте production-ready сервер** (gunicorn, uWSGI) вместо встроенного Flask сервера

### Пример запуска в продакшене с Gunicorn:

```bash
pip install gunicorn
gunicorn -w 4 -b 0.0.0.0:5001 app:app
```

## 🌐 Развертывание

### Локальная сеть

Чтобы другие пользователи в локальной сети могли получить доступ:

1. Найдите свой локальный IP: `ifconfig` (macOS/Linux) или `ipconfig` (Windows)
2. Запустите приложение: `python app.py`
3. Пользователи могут зайти по адресу: `http://ваш_IP:5001`

### Облачное развертывание

Приложение можно развернуть на:
- Heroku
- DigitalOcean
- AWS EC2
- Google Cloud Platform
- VPS-хостинге

**Важно**: Установите переменные окружения `SECRET_KEY` и `ENCRYPTION_KEY` на сервере!

## 📝 Лицензия

Этот проект создан в образовательных целях. Используйте на свой страх и риск.

## ⚖️ Disclaimer

Это приложение НЕ является финансовой консультацией. Все инвестиционные решения вы принимаете самостоятельно на свой страх и риск. Автор не несет ответственности за ваши финансовые решения или утерю данных.
